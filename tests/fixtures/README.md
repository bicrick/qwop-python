# Gold trace fixtures

Place gold trace JSON files here for cross-env parity tests. Naming:

- `gold_trace_seed<seed>_fps<frames_per_step>.json`

Example: `gold_trace_seed42_fps4.json`

See `doc/PARITY_GOLD_TRACE.md` for format and how to generate from qwop-gym or qwop-python.
